apiVersion: apps/v1
kind: Deployment
metadata: { name: order-service-26, labels: { app: order-service-26 } }
spec:
  replicas: 1
  selector: { matchLabels: { app: order-service-26 } }
  template:
    metadata: { labels: { app: order-service-26 } }
    spec: { containers: [{ name: order-service, image: nginx:1.27, ports: [{ containerPort: 80 }] }] }
