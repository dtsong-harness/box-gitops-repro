apiVersion: apps/v1
kind: Deployment
metadata: { name: order-service-28, labels: { app: order-service-28 } }
spec:
  replicas: 1
  selector: { matchLabels: { app: order-service-28 } }
  template:
    metadata: { labels: { app: order-service-28 } }
    spec: { containers: [{ name: order-service, image: nginx:1.27, ports: [{ containerPort: 80 }] }] }
