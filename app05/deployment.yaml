apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service-app05
  labels: { app: order-service-app05 }
spec:
  replicas: 1
  selector: { matchLabels: { app: order-service-app05 } }
  template:
    metadata: { labels: { app: order-service-app05 } }
    spec:
      containers:
      - name: order-service
        image: nginx:1.27
        ports: [{ containerPort: 80 }]
