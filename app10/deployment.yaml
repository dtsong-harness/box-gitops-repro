apiVersion: apps/v1
kind: Deployment
metadata: { name: order-service-10, labels: { app: order-service-10 } }
spec:
  replicas: 1
  selector: { matchLabels: { app: order-service-10 } }
  template:
    metadata: { labels: { app: order-service-10 } }
    spec: { containers: [{ name: order-service, image: nginx:1.27, ports: [{ containerPort: 80 }] }] }
