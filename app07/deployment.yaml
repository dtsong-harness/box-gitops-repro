apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service-app07
  labels: { app: order-service-app07 }
spec:
  replicas: 1
  selector: { matchLabels: { app: order-service-app07 } }
  template:
    metadata: { labels: { app: order-service-app07 } }
    spec:
      containers:
      - name: order-service
        image: nginx:1.27
        ports: [{ containerPort: 80 }]
