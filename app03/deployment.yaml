apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service-app03
  labels: { app: order-service-app03 }
spec:
  replicas: 1
  selector: { matchLabels: { app: order-service-app03 } }
  template:
    metadata: { labels: { app: order-service-app03 } }
    spec:
      containers:
      - name: order-service
        image: nginx:1.27
        ports: [{ containerPort: 80 }]
